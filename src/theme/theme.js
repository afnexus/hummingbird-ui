import { createTheme } from '@mui/material/styles';

export const muitheme = createTheme({
  palette: {
    primary: {
      main: '#800080',
    },
    secondary: {
      main: '#edf2ff',
    },
  },
});